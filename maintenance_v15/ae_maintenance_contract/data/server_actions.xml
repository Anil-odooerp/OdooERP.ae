<odoo>
    <data>

        <record id="model_sale_order_action_create_contract_invoice" model="ir.actions.server">
            <field name="name">Create Contract Invoice</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="binding_model_id" ref="sale.model_sale_order"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">action = records.action_create_sos_invoice()</field>
        </record>

    </data>
</odoo>
